<nav class="navbar navbar-expand-lg navbar-light bg-light" id="navigation-bar" >
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#collapsing-navbar">
      <span class="navbar-toggler-icon"></span>
  </button>
  <div class="navbar-collapse collapse" id="collapsing-navbar">
    <ul class="navbar-nav">
      <li class="nav-item active glyphicon">
          <%= link_to ' Home', root_path, class: 'glyphicon-home' %>
      </li>
    </ul>
    <ul class="navbar-nav  pull-right">
      <li class="nav-item glyphicon">
        <% if session[:order_id] %>
          <%= link_to " #{current_order.total} Checkout", order_path(session[:order_id]), class: 'glyphicon-shopping-cart', id: "order-icon" %>
        <% end %>
      </li>
      <% if current_user %>
        <li class="nav-item active">
          <span>Logged in as: <strong><%= current_user.email %></strong></span>
        </li>
        <li class="nav-item">
          <%= link_to 'Logout', destroy_user_session_path, method: :delete %>
        </li>
      <% else %>
        <li class="nav-item glyphicon">
          <%= link_to 'Log in', new_user_session_path unless current_page? new_user_session_path %>
          <%= link_to 'Sign up', new_user_registration_path unless current_page? new_user_registration_path %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
